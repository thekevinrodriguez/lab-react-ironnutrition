import { Button, Card, Col } from "antd";

const FoodBox = (props) => {};
return (
  <Col>
    <Card title=" " style={{ width: 230, height: 300, margin: 10 }}>
      <img src="{}" height={60} alt="" />
      <p> Calories: {food.Calories} </p>
    </Card>
  </Col>
);

export default FoodBox;
